- if resource.present? && resource.errors.any?
  = content_tag :ul, class: 'alert alert-danger', data: { resource_id: resource.id } do
    - resource.errors.full_messages.each do |msg|
      li = msg
