tr
  td class="body" = link_to question.body, question
  td = link_to t('edit'), edit_question_path(question)
  td = link_to t('delete'), question_path(question), method: :delete, data: { confirm:"#{t('confirm')}" }

